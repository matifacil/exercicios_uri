#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int main() 
{
    int a, b;
    cin >> a >> b;
    double price;
		price = a==1 ? 4 : a==2 ? 4.5 : a==3 ? 5 : a==4 ? 2 : 1.5;
    cout << "Total: R$ " << fixed << setprecision(2) << b*price << endl;
    
}
